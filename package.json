{
  "name": "the-paths",
  "version": "1.0.0",
  "description": "The Paths - System Interface, a gamified self-discipline tracker",
  "scripts": {
    "sync": "cap sync android",
    "build:android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/local-notifications": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
